Install client:
    tailwind
    vite
    router-react-dom
    npm install axios
    npm install cors

Install server:
    express
    nodemon
    npm install --save mysql2
    npm install bcrypt
    npm install crypto
    npm install jsonwebtoken
    npm install cookie-parser
    npm install dotenv
    npm install redis
    sudo apt update && sudo apt upgrade -y+
    sudo apt install redis-server -y
    *Instalar Redis en el servidor expuesto en el puerto default 6379*

#Redis
sudo service redis-server start  // Start redis
redis-cli ping // Test redis
sudo systemctl enable redis-server

WSL For linux environment inside Windows:
- path: /mnt/d/M.A.P.A/Miguel Pérez/Documentos/M.A.P.A/projects/CAZ/dev/caz-app-by-tgmapa

Pendientes: 
- Falta refresacar cookie - Completado
- Falta implementar acceso dinamico a endpoints segun tablas !!